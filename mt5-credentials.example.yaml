# Copy to mt5-credentials.yaml and fill in real values. That file is
# git-ignored - never commit real credentials.
#
# One profile per broker, selected by config.yaml's mt5.use: <profile>.
# Add another block here (e.g. fundednext:, exness:) and switch mt5.use to
# point at it - no other config.yaml changes needed.

fbs:
  login: 00000000
  password: "your-mt5-password"
  server: "YourBroker-Demo"
  terminal_path: "C:\\Program Files\\Your MT5 Terminal\\terminal64.exe"
